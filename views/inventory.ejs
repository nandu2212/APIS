<html>
    <head>
        <style>
          /* th, td {
  padding-top: 5px;
  padding-bottom: 5px;
  padding-left: 5px;
  padding-right: 5px;
}
table {
  border-spacing: 20px;
} */
.tb{
    border: 2px solid black;
}
        </style>
        <body>
            <table class="tb">
                <tr>
                 <th class="inventory">Inventory_id</th>
                 <th class="type">Inventory Type</th>
                 <th class="name">Item Name</th>
                 <th class="quantity">Available_quantity</th>
                </tr>
            <% for (let i=0;i<user.length;i++) {%>
                    <tr>
                     <td><%= user[i].Inventory_id %></td>
                     <td><%= user[i].InventoryType %></td>
                     <td><%= user[i].ItemName%></td>
                     <td><%= user[i].AvailableQuantity %></td>
                    </tr>      
            <% } %>
        </table>
        </body>
    </head>
</html>